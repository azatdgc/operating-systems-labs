# Example 01 – read() and write() System Calls

## Purpose
To demonstrate basic file input and output using read and write system calls.

## System Calls / Functions Used
- open()
- read()
- write()
- close()

## How It Works
The program opens a file and reads its content into a buffer.
Then it writes this content to standard output.

## How to Compile and Run
gcc main.c -o app
./app
